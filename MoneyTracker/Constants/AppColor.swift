import SwiftUI

/// 앱 전체 색상 상수 정의
enum AppColor {
    /// 메인 그린 (#00B894)
    static let primary = Color(hex: "#00B894")
    /// 골드 (#FDCB6E)
    static let secondary = Color(hex: "#FDCB6E")
    /// 지출 레드 (#E17055)
    static let accent = Color(hex: "#E17055")
    /// 배경 (#0D0D0D)
    static let background = Color(hex: "#0D0D0D")
    /// 보조 배경 (#1A1A2E)
    static let secondaryBackground = Color(hex: "#1A1A2E")
    /// 카드 배경 (#2D2D3F)
    static let cardBackground = Color(hex: "#2D2D3F")
    /// 기본 텍스트 (화이트)
    static let textPrimary = Color.white
    /// 보조 텍스트 (#A0A0A0)
    static let textSecondary = Color(hex: "#A0A0A0")
    /// 수입 색상 (#00B894)
    static let income = Color(hex: "#00B894")
    /// 지출 색상 (#E74C3C)
    static let expense = Color(hex: "#E74C3C")
}
